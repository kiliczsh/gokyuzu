name: Gokyuzu to PyPI and TestPyPI

on: workflow_dispatch

jobs:
  build:
    name: Build and publish Python 🐍 distributions 📦 to PyPI and TestPyPI
    runs-on: ubuntu-latest
    steps:
    - name: Not Implemented Yet
      run: >-
        echo "Not Implemented Yet"